<!DOCTYPE html>
<html>
<head>
  <title>Upload-Ergebnis</title>
  <style>
    body { font-family: sans-serif; padding: 2rem; }
    .success { color: green; }
    .fail { color: red; }
    pre { background: #f5f5f5; padding: 1rem; border-radius: 5px; }
  </style>
</head>
<body>
  <h1><%= success ? '✅ Upload erfolgreich!' : '❌ Upload fehlgeschlagen' %></h1>

  <% if (url) { %>
    <p>📺 <a href="<%= url %>" target="_blank">Zum Video</a></p>
  <% } %>

  <h2>Logs:</h2>
  <pre><%= logs.join('\n') %></pre>

  <p><a href="/admin/upload">⬅️ Zurück zum Upload</a></p>
</body>
</html>
