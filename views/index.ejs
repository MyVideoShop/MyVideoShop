<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= shopName %> – Startseite</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="dark">
  <header>
    <img src="/logo.avif" alt="Logo" class="logo" />
    <h1><%= shopName %></h1>
  </header>

  <main>
    <h2>Willkommen auf der Startseite!</h2>
    <p>Hier werden bald Videos angezeigt...</p>
  </main>

  <!-- Creator Login Button -->
  <button id="loginButton" class="corner-btn">Creator Login</button>

<!-- Button -->
<button onclick="document.getElementById('supportModal').style.display='block'">Support kontaktieren</button>

<!-- Modal -->
<div class="modal" id="supportModal">
  <div class="modal-content">
    <form action="/support/send" method="POST">
      <h2>Supportnachricht</h2>
      <input type="text" name="title" placeholder="Titel" required><br>
      <textarea name="description" placeholder="Beschreibung (10–200 Wörter)" minlength="10" maxlength="200" required></textarea><br>
      <button type="submit">Senden</button>
      <button type="button" onclick="document.getElementById('supportModal').style.display='none'">Schließen</button>
    </form>
  </div>
</div>

  <!-- Login Modal -->
  <div id="loginModal" class="modal hidden">
    <div class="modal-content">
      <h2>Creator/Admin Login</h2>
      <form id="loginForm">
        <input type="text" name="name" placeholder="Name*" required />
        <input type="email" name="email" placeholder="E-Mail (nur für Bewerbung)" />
        <input type="password" name="password" placeholder="Passwort*" required />

        <div id="formMessage" class="form-message"></div>

        <div class="modal-buttons">
          <button type="submit" name="action" value="apply">Bewerben</button>
          <button type="submit" name="action" value="login">Einloggen</button>
        </div>
      </form>
    </div>
  </div>

  <script src="/js/script.js"></script>
</body>
</html>
